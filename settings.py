memory_size = 2048 # Default size. You don't need to worry about this. It's mainly a simulated memory-size, for the purposes of the joke that brainf**k is.

help_menu_text = """
Invalid argument.

Usage:

(fields marked with * are imperative)

python main.py *[filein]: 
--compile/-c (compiles with gcc) *[fileout]
--save/-s (saves to harddisk) *[fileout]
--print/-p (prints to stdout)
"""